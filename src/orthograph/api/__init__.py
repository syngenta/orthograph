"""Consumer-facing capability surface.

* :mod:`orthograph.api.model`         — in-memory operations (load, save, validate)
* :mod:`orthograph.api.database`      — live-database operations
  (inspect, validate, query, execute)
* :mod:`orthograph.api.visualization` — rendering
  (render_model, render_profile, render_result, display)

Usage::

    from orthograph.api import model, database, visualization
"""
