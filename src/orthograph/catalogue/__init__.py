"""Typed query catalogue — ReadQuery, WriteQuery, Executor, ReadPort, Backend.

Queries are Python classes. Return types are statically known.
No string-key dispatch. No YAML (see epic E16 OPEN DECISION).
"""
