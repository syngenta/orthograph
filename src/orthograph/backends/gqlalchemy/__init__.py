"""GQLAlchemy backend adapter (ORM).

Requires the ``gqlalchemy`` extra (``pip install orthograph[gqlalchemy]``).
Import symbols from their true modules, e.g.::

    from orthograph.backends.gqlalchemy.client import GqlAlchemyClient
"""
